# Workspace

All code projects live here, one folder per project, each with its own README (what it is, how to run, status, debt).

Created and managed by the build skill (`.claude/skills/build/SKILL.md`).
