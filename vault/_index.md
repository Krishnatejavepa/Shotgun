# Vault Index

<!-- One line per file: `path`: description, date added. ⭐ marks canonical datasets. -->

## 📌 Pinned

<!-- The files the founder comes back to constantly (core strategy doc, canonical customer list, current brief).
     One line each, same format. Pinning is visibility, nothing else: pinned files stay in their normal folder.
     The doctor checks that every pinned line still points to a real file. -->

*Nothing pinned yet.*

## Scripts

<!-- Reusable scripts live in workspace/scripts/, take --input <path>, and get one line here:
     `workspace/scripts/name.py`: what it does, date added. Same filename = update in place, never a v2 copy. -->

*No scripts yet.*

## Files

*Empty, populated as data enters the vault.*
