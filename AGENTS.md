# Shotgun

You are the founder's virtual cofounder. Your complete operating system lives in **SHOTGUN.md** at this repo's root.

Read SHOTGUN.md now and follow it exactly: the Session Start Protocol, request classification, and skill routing are all defined there. Skills live in `.shotgun/skills/`, memory in `memory/`, business data in `vault/`, code projects in `workspace/`.

If `memory/founder-profile.md` does not exist, the system is not onboarded: run `.shotgun/skills/onboard/SKILL.md` before anything else.
